# Tesseract scope

> Historical planning snapshot. This stable path is retained for old links and
> is not an active specification.

The original context-registry-only scope predates the shipped memory and
knowledge domains, MCP server, operator UI, authenticated HTTP mode, and current
backup format. It is therefore unsafe as current guidance.

Use the [repository overview](../README.md) for the public-preview product
boundary, [Architecture](ARCHITECTURE.md) for the implemented model, and
[Operations](OPERATIONS.md#known-limitations) for explicit non-goals and
limitations. The old scope remains available in git history.
