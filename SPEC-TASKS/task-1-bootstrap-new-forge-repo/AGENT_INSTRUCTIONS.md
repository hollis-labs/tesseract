# Agent Instructions

Operate in a Forge-managed workflow (Inception pattern).

- Prefer minimal changes and clear documentation.
- Produce all required deliverables under `outputs/`.
- If assumptions are needed (repo name, remote URL), record them in `outputs/open-questions.md`.
- Do not start implementation of the service; scaffold only.
